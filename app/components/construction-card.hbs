 <li class="flex flex-col px-6 lg:px-8 p-8 bg-gray-100 border border-gray-300 h-full rounded">
    <div class="mb-6 flex-1">
      <h3 class="text-base sm:text-lg md:text-xl font-bold  mb-3 text-gray-900">{{@title}}</h3>
      <p class="text-gray-700 text-sm font-medium">{{@text}}</p>
    </div>
    <div class="flex items-center justify-between">
      <div class="hover:bg-gray-200">
        {{yield}}
      </div>
      <img class="w-24" src={{@illustration}} alt={{@title}}>
    </div>
  </li>